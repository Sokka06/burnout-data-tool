# VDB variable definitions for Burnout 3 Takedown.
# Default values in the VDB file are accessed using the hash, which comes from hashing together name, path and filename.
# Data types 0 = int, 1 = float, 3 = bool etc...

# functions where variables are registered
# FUN_00208b60 
# FUN_00209670

definitions:
  - hash: 1231424
    type: 0
    name: "Mass (Kg)"
    path: "Physics/Vehicle"
    fileName: "../Export/ValueDB/VehiclePhysics/%s.cfg" # %s is replaced with the uncompressed ID of the car, e.g. COMPCAR1.

